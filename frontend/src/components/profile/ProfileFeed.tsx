import React, { useState, useEffect } from 'react';

// types imports
import { ProfileViewProps } from '@/types/props/props-types';

// export const ProfileFeed = ({ user, isLoading, isOwnProfile}: ProfileViewProps) => {
export const ProfileFeed = () => {
    return (
        <div className="flex flex-col items-center justify-center p-4">
            <p>Profile Feed</p>
            <p>Feed will appear here soon.</p>
        </div>
    );
};